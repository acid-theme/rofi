name: Preview

# Generated. The steps live in the hub so every port shares them; this file only
# says when to run them.

on:
  push:
    paths:
      - "acid-acetic.rasi"
      - "acid-citric.rasi"
      - "preview/**"
  workflow_dispatch:

jobs:
  preview:
    uses: acid-theme/acid/.github/workflows/port-preview.yml@main
    permissions:
      contents: write
